name: UFS Custom Checks

on:
  push:
    branches: ['**']
  pull_request:
    branches: [develop]

defaults:
  run:
    shell: bash -eo pipefail {0}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  enforce-standards:
    name: Enforce Repo Standards
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for Bash Octal Traps
        run: |
          echo "Scanning for zero-initialized variables used unprotected in arithmetic expansions..."
          echo "Scanning tests/default_vars.sh and tests/tests/*"
          UNPROTECTED=$(perl -ne '
            our %safe_arith_vars;
            our %zero_init_vars;
            my $line = $_;
            next if $line =~ /^\s*#/;
            my $flagged = 0;

            if ($line =~ /^\s*(?:export\s+)?([A-Za-z_][A-Za-z0-9_]*)=(.+)$/) {
              my ($var, $rhs) = ($1, $2);
              $rhs =~ s/\s+#.*$//;
              $rhs =~ s/^\s+|\s+$//g;

              # Track only variables explicitly initialized with a zero-prefixed numeric literal.
              if ($rhs =~ /^(?:"\s*0[0-9]*\s*"|'\s*0[0-9]*\s*'|0[0-9]*)$/) {
                $zero_init_vars{$var} = 1;
              }
            }

            if ($line =~ /^\s*(?:export\s+)?([A-Za-z_][A-Za-z0-9_]*)=\$\(\((.*?)\)\)/) {
              $safe_arith_vars{$1} = 1;
            }

            while ($line =~ /\$\(\((.*?)\)\)/g) {
              my $expr = $1;

              # Case 1: explicit variable expansion ($VAR or ${VAR}) not protected by 10#
              while ($expr =~ /(\$\{?([A-Za-z_][A-Za-z0-9_]*)\}?)/g) {
                my $var = $2;
                next if !$zero_init_vars{$var};

                my $start = $-[1];
                my $before = substr($expr, 0, $start);
                if ($before !~ /10#\s*$/) {
                  $flagged = 1;
                  last;
                }
              }

              # Case 2: bare variable names in arithmetic context (e.g., SHOUR*3600)
              # These are also vulnerable to octal interpretation and should be flagged.
              if (!$flagged) {
                while ($expr =~ /\b([A-Za-z_][A-Za-z0-9_]*)\b/g) {
                  my $start = $-[1];
                  my $before = substr($expr, 0, $start);

                  # Skip identifiers that are part of $VAR or ${VAR}.
                  my $prev1 = $start > 0 ? substr($expr, $start - 1, 1) : "";
                  my $prev2 = $start > 1 ? substr($expr, $start - 2, 2) : "";
                  next if $prev1 eq q|$| || $prev2 eq q|${|;

                  # Skip identifiers that are part of base literals like 16#FF.
                  next if $before =~ /\d+#\s*$/;

                  # Only enforce this for vars explicitly initialized with a leading zero literal.
                  next if !$zero_init_vars{$1};

                  # Skip variables that were already derived from arithmetic in this file.
                  next if $safe_arith_vars{$1};

                  $flagged = 1;
                  last;
                }
              }

              last if $flagged;
            }
            print "$ARGV:$.:$_" if $flagged;
          ' tests/default_vars.sh tests/tests/*)

          if [[ -n "$UNPROTECTED" ]]; then
            echo "::error::🚨 OCTAL TRAP DETECTED! Zero-initialized variables inside math expansions must use the 10# prefix."
            echo "The following lines use zero-initialized variables without 10# protection:"
            echo "$UNPROTECTED"
            exit 1
          fi

          echo "✅ All zero-initialized variable math expansions are safely protected!"

      # - name: Ban 'export' in configuration files
      #   run: |
      #     echo "Scanning for illegal 'export' declarations in tests/default_vars.sh and tests/tests/*..."
      #     mapfile -d '' CHECK_FILES < <(find tests/default_vars.sh tests/tests -type f -print0)
      #
      #     if [[ ${#CHECK_FILES[@]} -eq 0 ]]; then
      #       echo "::error::No files found to scan for export declarations."
      #       exit 1
      #     fi
      #
      #     ILLEGAL_EXPORTS=$(perl -ne 'next if /^\s*#/; print "$ARGV:$.:$_" if /^\s*export\b(?=\s|$)/' "${CHECK_FILES[@]}")
      #
      #     if [[ -n "$ILLEGAL_EXPORTS" ]]; then
      #       echo "::error::🚨 EXPORT DETECTED! Do not use 'export' in tests/default_vars.sh or tests/tests/*."
      #       echo "The following lines contain illegal exports:"
      #       echo "$ILLEGAL_EXPORTS"
      #       exit 1
      #     else
      #       echo "✅ No illegal exports found!"
      #       exit 0
      #     fi
