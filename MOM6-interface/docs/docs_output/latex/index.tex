\chapter{MOM6 Output Logging User Guide}
\hypertarget{index}{}\label{index}\index{MOM6 Output Logging User Guide@{MOM6 Output Logging User Guide}}
\label{index_md__2work_2noaa_2nems_2dworthen_2ufs__dw_2MOM6-interface_2docs_2feature__guide}%
\Hypertarget{index_md__2work_2noaa_2nems_2dworthen_2ufs__dw_2MOM6-interface_2docs_2feature__guide}%
 The MOM6 output logging feature is designed to track the completion of MOM6 history and restart files during a model run. This feature is specific to UWM operational requirements and configurations (eg specific output frequencies in hours) and may break if used outside the scope of intended use.

The feature is enabled by adding a namelist to the model {\ttfamily input.\+nml} which can be used to define the history output frequencies, filenames and types (snapshot or averages) which are to be tracked. The user is responsible for ensuring that these settings match the contents of the {\ttfamily diag\+\_\+table} in use by the model. From the user-\/configuration, one or more tracking Alarms are enabled at the desired frequencies. When an Alarm rings, a filename is constructed referencing the model time and the initial state of the file is recorded. Depending on the characteristics of the file at creation, the criteria to declare a file complete is defined.

The file state will be checked at each suceeding Model\+Advance until the appropriate completion criteria is met\+: either when the unlimited dimension in the file is greater than zero or when the unlimited dimension is greater than zero and the filesize is larger than the initial size. When a file is determined to be complete, a log file is recorded containing the forecast hour, the valid time, the name of the output file and the last completed restart file. The log file can then be used by any related workflow (e.\+g. \href{https://github.com/NOAA-EMC/global-workflow}{\texttt{ global-\/workflow}}).

To illustrate the concepts implemented in the output logging feature, consider the following diagram\+:



In this case, MOM6 is providing 6-\/hourly output; the model begins at hour=00 and runs for 30 hours; the output alarm will ring every 6 hours. It is important to remember that alarms ring at the Model\+Advance next\+Time. If the model in this case is running with a 30 minute coupling frequency, the alarm at hour=12\+:00 will ring when the model\+Advance curr\+Time = 11\+:30 and the Model\+Advance next\+Time = 12\+:00.

MOM6 history files for time-\/averaged output are timestamped at the middle of the averaging window. The first MOM6 history output file produced is the hour = 03 file, representing the average between hours 0 and 6. It will be created when the alarm rings at next\+Time = 12\+:00 and be completed on the next timestep (when the curr\+Time = 12\+:00). That file sequencing pattern holds until the model stop time. At finalization, there are two final history files that are written\+: the "{}pending"{} file (h = 21) as well as the final h = 03 file.

Restarts for MOM6 are written by MOM6 directly, not using FMS as for the history files. Model restarts are written, complete, when the Model\+Adance next\+Time matches a requested hour. In the above diagram, restarts written at hour = 12 will be when the Model\+Advance curr\+Time = 11\+:30 and the Model\+Advance next\+Time = 12\+:00.

The output log file written at forecast hour = 18 for this case ({\ttfamily 20111001.\+180000.\+mom6.\+06h}) would contain the following information\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{completed:\ mom6.06h}
\DoxyCodeLine{forecast\ hour:\ \ \ \ 18.000}
\DoxyCodeLine{valid\ time:\ \ \ \ \ 2011\ \ \ \ \ \ 10\ \ \ \ \ \ \ 1\ \ \ \ \ \ 18\ \ \ \ \ \ \ 0\ \ \ \ \ \ \ 0}
\DoxyCodeLine{last\ output:\ ./MOM6\_OUTPUT/ocn\_2011\_10\_01\_09\_00.nc}
\DoxyCodeLine{last\ restart:\ \ \ \ \ 2011\ \ \ \ \ \ 10\ \ \ \ \ \ \ 1\ \ \ \ \ \ 12\ \ \ \ \ \ \ 0\ \ \ \ \ \ \ 0}

\end{DoxyCode}


In the above case, since the log file for both the h = 21 and h = 03 file are both written at the same model hour (FH = 30 in this case), the log for the very last file is denoted with {\ttfamily lstop} appended to the file name ({\ttfamily 20111002.\+060000.\+mom6.\+lstop.\+06h}).

\begin{DoxyNote}{Note}
The above sample log file also highlights the essential feature of the output sequencing. At FH = 18, the only output files which are complete are those at hours 03 and 09; the history file for the interval which {\itshape ends} at FH = 18 (the h = 15 file) has not yet been written.
\end{DoxyNote}
\hypertarget{index_autotoc_md1}{}\doxysection{\texorpdfstring{Design and Function}{Design and Function}}\label{index_autotoc_md1}
The outputlog feature consists of two fortran modules in {\ttfamily config\+\_\+src/drivers/nuopc\+\_\+cap}\+:


\begin{DoxyItemize}
\item \doxylink{mom__cap__outputlog_8F90}{mom\+\_\+cap\+\_\+outputlog.\+F90}
\item \doxylink{mom__outputlog__methods_8F90}{mom\+\_\+outputlog\+\_\+methods.\+F90}
\end{DoxyItemize}

and three type structures \+:


\begin{DoxyItemize}
\item \doxylink{structmom__outputlog__methods_1_1outputlog__config__type}{mom\+\_\+outputlog\+\_\+methods\+::outputlog\+\_\+config\+\_\+type} \+: the time-\/invariant configuration at each tracked frequency
\item \doxylink{structmom__outputlog__methods_1_1outputlog__state__type}{mom\+\_\+outputlog\+\_\+methods\+::outputlog\+\_\+state\+\_\+type} \+: the time-\/evolving state at each tracked frequency
\item \doxylink{structmom__outputlog__methods_1_1outputlog__modeltime__type}{mom\+\_\+outputlog\+\_\+methods\+::outputlog\+\_\+modeltime\+\_\+type}\+: the model time state during each Model\+Advance
\end{DoxyItemize}\hypertarget{index_autotoc_md2}{}\doxysubsection{\texorpdfstring{Configuration}{Configuration}}\label{index_autotoc_md2}
The output log feature is enabled with a {\ttfamily MOM\+\_\+outputlog\+\_\+nml} namelist added to the {\ttfamily input.\+nml} which will populate the outputlog\+\_\+config\+\_\+type. For example, the following values will request tracking 6-\/hourly files, which (using the {\ttfamily diag\+\_\+table}) have a filename prefix of {\ttfamily ocn} and are defined as time-\/averaged values. No debug information will be added to the stdout file.


\begin{DoxyCode}{0}
\DoxyCodeLine{\&MOM\_outputlog\_nml}
\DoxyCodeLine{\ \ outputlog\_fh\ =\ 6}
\DoxyCodeLine{\ \ outputlog\_fnameprefix\ =\ 'ocn'}
\DoxyCodeLine{\ \ outputlog\_treduce\ =\ 'average'}
\DoxyCodeLine{\ \ outputlog\_debug\ =\ .false.}
\DoxyCodeLine{/}

\end{DoxyCode}


At a minimum, the desired tracking frequency must be provided, which will default to time-\/averaged files with the file prefix {\ttfamily ocn}. The following namelist will be treated identically as that above


\begin{DoxyCode}{0}
\DoxyCodeLine{\&MOM\_outputlog\_nml}
\DoxyCodeLine{\ \ outputlog\_fh\ =\ 6}
\DoxyCodeLine{/}

\end{DoxyCode}


The following rules apply the namelist options for output logging\+:\hypertarget{index_autotoc_md3}{}\doxysubsubsection{\texorpdfstring{Logging Frequency}{Logging Frequency}}\label{index_autotoc_md3}
File tracking can be enabled for 1,3,6 or 24 hourly files only. Multiple frequencies can be requested and the listed order is immaterial. However, each logging frequency must be uniquely defined. For example, 6-\/hourly average files and 6-\/hourly snapshot files are not allowed but 6-\/hourly average and 3-\/hourly snapshot files are.

\begin{DoxyNote}{Note}
Because it is intended for operational purposes, logging frequency is intentionally set to log 3 and 6 hour frequencies relative to the operational forecast windows. This means that a 6 hour tracking frequency is always implemenented as starting at one of hours 00,06,12 or 18. A similar rule applies to a 3 hour tracking frequency (i.\+e, 00,03,06 etc). Tracking at 24 hour intervals is for consecutive 24 hour periods. For example, hour 09 on day one through to hour 09 on day 2.
\end{DoxyNote}
\hypertarget{index_autotoc_md4}{}\doxysubsubsection{\texorpdfstring{Filename Prefix}{Filename Prefix}}\label{index_autotoc_md4}
If a single frequency is requested, no filename prefix is required. A default prefix of {\ttfamily ocn} will be used. It is the user\textquotesingle{}s responsibility that the default matches the specification in the {\ttfamily diag\+\_\+table}. Otherwise, the filename prefix must be set to the actual filename prefix called for in the {\ttfamily diag\+\_\+table}. If more than a single frequency is requested, the user must provide filename prefixes for all frequencies (again ensuring matches to the {\ttfamily diag\+\_\+table}). The filename prefix can have a maximum length of 12 (not including the trailing underscore, which will be appended).\hypertarget{index_autotoc_md5}{}\doxysubsubsection{\texorpdfstring{File Time Reduction}{File Time Reduction}}\label{index_autotoc_md5}
Either instantaneous (snapshot) files or time averaged files are supported. These are specified by the namelist {\ttfamily treduce} settings of {\ttfamily none} and {\ttfamily average}, respectfully. The file name format for time averaged output is asssumed to be timestamped with the mid-\/point of the averaging window (i.\+e. 09 for the 6h-\/12h average). For snapshot output, the timestamp will be the time of the snapshot. The user is responsible for ensuring that the diag\+\_\+table in use matches these definitions.\hypertarget{index_autotoc_md6}{}\doxysubsubsection{\texorpdfstring{Feature Debugging}{Feature Debugging}}\label{index_autotoc_md6}
When enabled in the namelist, debugging print statements will be written to standard out. These statements track the state of the feature tracking at each step through the Model\+Advance. Print statements will be pre-\/pended with an identifing routine, for example {\ttfamily MOM\+\_\+cap\+:(track\+\_\+freqn)}. Utilizing this feature for a case with tracking of 6-\/hourly average output produces\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{304:\ MOM\_cap:(track\_freqn)\ \ \ \ ./MOM6\_OUTPUT/ocn\_2021\_03\_22\_21\_00.nc\ exists\ 2021-\/03-\/23T05:30:00\ \ 2021-\/03-\/23T06:00:00\ not\ complete,\ chkflag\ \ T\ \ \ \ \ \ \ \ \ 9415276\ \ \ \ \ \ \ \ \ 9415276\ \ \ \ 1}
\DoxyCodeLine{304:\ MOM\_cap:(track\_freqn)\ \ \ \ ./MOM6\_OUTPUT/ocn\_2021\_03\_22\_21\_00.nc\ exists\ 2021-\/03-\/23T06:00:00\ \ 2021-\/03-\/23T06:30:00\ \ \ \ \ complete,\ chkflag\ \ F\ \ \ \ \ \ \ \ \ 9415276\ \ \ \ \ \ \ \ 90532460\ \ \ \ 1}
\DoxyCodeLine{304:\ MOM\_cap:(track\_freqn)\ \ \ \ ./MOM6\_OUTPUT/ocn\_2021\_03\_22\_21\_00.nc\ exists\ 2021-\/03-\/23T06:30:00\ \ 2021-\/03-\/23T07:00:00\ \ \ \ \ complete,\ chkflag\ \ F\ \ \ \ \ \ \ \ \ 9415276\ \ \ \ \ \ \ \ 90532460\ \ \ \ 1}
\DoxyCodeLine{304:\ MOM\_cap:(track\_freqn)\ \ \ \ ./MOM6\_OUTPUT/ocn\_2021\_03\_22\_21\_00.nc\ exists\ 2021-\/03-\/23T07:00:00\ \ 2021-\/03-\/23T07:30:00\ \ \ \ \ complete,\ chkflag\ \ F\ \ \ \ \ \ \ \ \ 9415276\ \ \ \ \ \ \ \ 90532460\ \ \ \ 1}

\end{DoxyCode}


As the model advances, the feature scans for a specific filename. When that file is found, the debug print will indicate that the file is present at a given {\ttfamily model\+Advance} time pair (the curr\+Time and the next\+Time). The completion state is given, as well as the status of the {\ttfamily chkfile\+\_\+next\+Advance} logical. The next two columns report the size of the file when created and the current size of the file. The final column reports the length of the unlimited dimension. Once the file is determined complete, the checking flag flips to false and no further inquires on the state of that particular file will be made.\hypertarget{index_autotoc_md7}{}\doxysubsubsection{\texorpdfstring{Alarm Initialization}{Alarm Initialization}}\label{index_autotoc_md7}
An alarm is intialized at each desired tracking frequency. As noted previously, alarms are set to ring at multiples of the tracking frequency and intialized with a time-\/offset to ensure that they ring at on intervals associated of the operational forecast hours.\hypertarget{index_autotoc_md8}{}\doxysubsubsection{\texorpdfstring{IO-\/layout}{IO-layout}}\label{index_autotoc_md8}
When IO-\/layout is enabled, the root PE associated with the IO domain is co-\/located with the root PE of the computation domain. Each IO-\/domain will produce a history file for the domain; the file names will be appended with the IO-\/domain number, for example {\ttfamily .nc.\+0000}. The number of history files expected obtained using the internal MOM6 function {\ttfamily mpp\+\_\+get\+\_\+io\+\_\+domain\+\_\+layout}. For a single IO-\/domain, no file name suffix is used; when IO-\/layout is in use, only the root IO task associated with the {\ttfamily .nc.\+0000} file will be queried for the model state.\hypertarget{index_autotoc_md9}{}\doxysubsection{\texorpdfstring{File Tracking Sequence}{File Tracking Sequence}}\label{index_autotoc_md9}
\hypertarget{index_autotoc_md10}{}\doxysubsubsection{\texorpdfstring{File State at Creation}{File State at Creation}}\label{index_autotoc_md10}
When an alarm rings for a frequency, the filename for the expected history output is constructed and the initial state of the file, if present, is obtained. Two characteristics of the file are obtained\+: the length of the unlimited dimension and the inital size of the file. For configurations using MOM6 and a data atmosphere (DATM), the inital unlimited dimension when the file is created is found to be 0. For active atmosphere configurations, an initial unlimited dimension length of 1 is observed. In these cases, file size will also be used to determine file completion. This is enabled using the state variable {\ttfamily use\+\_\+filesize}.

Once the initial file state is obtained at ring time, a flag will be set to check the file on the next Model\+Advance. The file will continue to be checked on each advance until it is reported complete. In practice, the file is completed on the next Model\+Advance after creation.\hypertarget{index_autotoc_md11}{}\doxysubsubsection{\texorpdfstring{Determining File Completion}{Determining File Completion}}\label{index_autotoc_md11}
Depending on the state variable {\ttfamily use\+\_\+filesize}, a file is determined to be complete in one of two ways. If the initial unlimited dimension was zero (a DATM case), file completion occurs when the file state obtains an unlimited dimension of 1. Otherwise, the secondary criteria of file size will be used. The criteria here is only that the file size is greater than the initial size.

Once a file is determined complete, the check flag for the next Advance is turned off and the state is updated to store the last restart file written.\hypertarget{index_autotoc_md12}{}\doxysubsubsection{\texorpdfstring{Finalization}{Finalization}}\label{index_autotoc_md12}
When the model completes in the Model\+Finalization phase, two calls to the tracking feature are required. Both occur after the IO has been shut down. The first call checks the status of the penultimate file; the file which would have completed at the next Model\+Advance timestep. Since in this case, there will be no next timestep, this \textquotesingle{}pending\textquotesingle{} file is completed by the IO shutdown. The final file, for the last averaging window, is also completed during the IO shutdown.\hypertarget{index_autotoc_md13}{}\doxysubsubsection{\texorpdfstring{Restart Pairing}{Restart Pairing}}\label{index_autotoc_md13}
The history files for MOM6 are at specific cadences, e.\+g. 6-\/hourly. The restart files, however, can be written at either specific (but different) cadences or at specific forecast hours. There is in general no simple {\itshape a priori} relationship between the writing of restarts and the writing of history files. Therefore, the concept of \textquotesingle{}restart pairing\textquotesingle{} is used to refer to tracking the last restart file which was written at the time a history file is written. The paired restart is written to the log file produced by the feature.

The following example is based on the gfsv17 IAU regression test. History output for MOM6 is set to 6-\/hourly averages output and restarts written at specified hours. In this case, the model is restarting with {\ttfamily FHROT = 3} and since IAU is active, MOM6 averaging begins at {\ttfamily 03-\/22-\/12}. The restarts are requested at {\ttfamily restart\+\_\+interval\+: 6 24 45 78}.\hypertarget{index_autotoc_md14}{}\doxysubsection{\texorpdfstring{File Tracking Example}{File Tracking Example}}\label{index_autotoc_md14}
 